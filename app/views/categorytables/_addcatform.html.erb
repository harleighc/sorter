<%= form_for(@categorytable) do |f| %>
  <% if @categorytable.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@categorytable.errors.count, "error") %> prohibited this categorytable from being saved:</h2>

      <ul>
      <% @categorytable.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.hidden_field :user_id  , :user_id => current_user.id %>
  </div>

<div class="field">
  <%= f.hidden_field :system, :value => false %>
  </div>

<div class="field">
  <%= f.hidden_field :owner ,  :value => "user" %>
  </div>

<div class="field">
  <%= f.hidden_field :rank ,  :value => 0 %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>